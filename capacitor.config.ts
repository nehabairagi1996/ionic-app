import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MyStorageNotificationApp',
  webDir: 'www'
};

export default config;
